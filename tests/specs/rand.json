[
  {
    "name": "/rand prompts for minimum",
    "steps": [
      {
        "send": { "text": "/rand" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "What's the minimum number?" } }
        ]
      }
    ]
  },
  {
    "name": "Custom button from menu prompts for minimum",
    "steps": [
      {
        "send": { "callback": "rand:start" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "What's the minimum number?" } }
        ]
      }
    ]
  }
]
