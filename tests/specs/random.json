[
  {
    "name": "/random prompts for minimum",
    "steps": [
      {
        "send": { "text": "/random" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "What's the minimum number?" } }
        ]
      }
    ]
  }
]
